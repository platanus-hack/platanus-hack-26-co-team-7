"""AI reports module (openspec/architecture.md, "Reportes IA"): deterministic
SQL snapshot + real UNGRD gov actions -> LLM narration -> schema v1
validation -> persist -> WebSocket broadcast. The LLM only narrates given
data; it never computes figures."""
