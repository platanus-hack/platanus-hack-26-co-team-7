node_modules/
.expo/
dist/
web-build/

# Generated by `expo prebuild`. Regenerate instead of committing:
#   npx expo prebuild --platform android --clean
# Leading slash matters: modules/ziro-relay/android/ is source and must NOT be ignored.
/android/
/ios/

# Gradle output for the local Expo modules. The sources under modules/*/android/
# are tracked, but their build/ directories are not.
modules/*/android/build/

*.log
.DS_Store

.atl

package-lock.json
