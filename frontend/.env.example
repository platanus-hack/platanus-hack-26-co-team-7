# Public build-time configuration for the Expo application.
# Supply the production API URL through EAS or the deployment environment before building.
EXPO_PUBLIC_API_BASE_URL=https://api.example.com
# Demo-only UI control. This is public build configuration, never a secret.
EXPO_PUBLIC_DEMO_TRIGGER_ENABLED=false
